import SupplierLoginForm from "@/components/supplierPortal/SupplierLoginForm";

export const metadata = {
  robots: { index: false, follow: false },
};

export default function SupplierLoginPage() {
  return <SupplierLoginForm />;
}
